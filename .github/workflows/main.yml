on: [push]

jobs:
    deploy_job:
    runs-on: ubuntu-latest
    name: deploy
    steps:
        - name: Checkout
        uses: actions/checkout@v2
        - name: deploy file
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
            username: 'niki'
            server: '185.128.246.111'
            password: ${{ secrets.FTP_PASSWORD }} 
            remote_path: '/var/www/html'